#pragma once

namespace flysq {
float constrain(float val, float limit_up_bound, float limit_low_bound);
float max(float val1, float val2);
float min(float val1, float val2);
float abs(float val);
}  // namespace flysq
